<!DOCTYPE html>
<html>
  <head>
    <title>Calculator</title>
  </head>
  <body>
    <h1>Calculator</h1>
    <form action="/calculate" method="post">
      <input type="text" name="expression" placeholder="Enter an expression" value="<%= expression || '' %>">
      <button type="submit">Calculate</button>
    </form>
    <% if (result) { %>
      <p>Result: <%= result %></p>
    <% } %>
  </body>
</html>
